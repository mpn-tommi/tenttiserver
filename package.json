{
  "name": "tenttiserver",
  "version": "1.0.0",
  "description": "* tenttiserver - a server for questions\r Server implementation for a test application. The client included as a subtree.",
  "main": "dist/index.js",
  "scripts": {
    "start:dev": "nodemon dist/index.js",
    "build:dev": "tsc --watch --preserveWatchOutput",
    "dev": "concurrently \"npm:build:dev\" \"npm:start:dev\"",
    "test": "jest --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ECL-2.0",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "@types/jest": "^24.0.23",
    "@types/node": "^12.12.14",
    "concurrently": "^5.0.0",
    "jest": "^24.9.0",
    "nodemon": "^2.0.1",
    "supertest": "^4.0.2",
    "ts-jest": "^24.2.0",
    "ts-node": "^8.5.4"
  }
}
